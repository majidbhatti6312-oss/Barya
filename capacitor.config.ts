import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.obaidapp.iea.barya',
  appName: 'د اسلامي امارت د بريا راز',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
